@model IEnumerable<CrudApp.Models.Accreditation>

@{
    ViewData["Title"] = "Accreditation";
}

<h1>Accreditation</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.engineerID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.isAccredited)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.engineerID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.isAccredited)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MIPID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MIPID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MIPID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
